# Scripts

The main analysis is distributed as notebooks and R Markdown files because the
original workflow was developed step-by-step. Reusable helper code is stored in
`../src/`.

If you prefer a command-line workflow, this folder can be used for future
wrappers that execute selected notebook or R Markdown steps.
